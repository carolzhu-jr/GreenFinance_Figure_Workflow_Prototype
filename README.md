Added minimal multi-panel support to the figure grammar. Use spec.panels.layout (rows,cols) and spec.panels.panels list with row/col positions and per-panel marks. The generator divides the plot area into a grid, renders axes and marks per panel, and supports optional panel titles. Tests/examples included: pipeline_b/data/figure_spec_panels.json and tests/test_panels.py
